# dLLMSpec Pages 维护约束

## 实验报告页面

- 新增或改造“实验报告”类型页面时，必须以 `dflash-multistep/`（线上参考：<https://mokomoko05.github.io/dLLMSpec_pages/dflash-multistep/>）为唯一的字体、排版和 HTML 结构基准。
- 沿用其 Times New Roman 论文式字体体系、7.2 英寸正文宽度、字号/行距/留白、表格、代码块、链接、响应式与打印样式；不要为同类报告另建不一致的视觉体系。
- 页面结构至少包含：顶部导航、标题/副标题/实验元信息、摘要、页内目录、带稳定 `id` 的编号正文、原始记录、页脚。目录链接必须与 section `id` 一一对应。
- 每份实验报告放在独立目录中，使用 `index.html` 和 `styles.css`。优先复制或复用 `dflash-multistep/styles.css`；确需扩展时只做最小增量。
- 新增报告时，必须同步加入根目录 `index.html` 的“内容目录”，包含报告标题、简述和日期，并保持现有目录条目格式与时间顺序。
- 报告展示标题必须在根目录条目、页面 `<title>` 和页面 `<h1>` 中保持一致；更详细的阶段范围放在副标题或摘要，不要堆入主标题。
